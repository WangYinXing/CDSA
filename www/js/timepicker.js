(function(){
    'use strict';

    function createTimePicker(container) {
        $(container).append($("<div class='timePickerContainer'></div"));

        $(".timePickerContainer").append($("<div class='wheel'></div>"));
    }


})();